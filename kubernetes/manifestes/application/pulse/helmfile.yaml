repositories:
  - name: pulse
    url: https://rcourtman.github.io/Pulse/

helmDefaults:
  createNamespace: true
  wait: true
  timeout: 600

## Document separator to satisfy Helmfile:
---
namespace: pulse

releases:
  - name: pulse
    chart: pulse/pulse
    version: 4.33.1
    values:
      - values/pulse.yaml
