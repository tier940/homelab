repositories:
  - name: pulse
    url: https://rcourtman.github.io/Pulse/

helmDefaults:
  createNamespace: true
  wait: true
  timeout: 600

## Document separator to satisfy Helmfile:
---
releases:
  - name: pulse
    namespace: pulse
    chart: pulse/pulse
    version: 4.32.3
    values:
      - values/pulse.yaml
