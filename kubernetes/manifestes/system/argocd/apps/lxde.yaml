apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: lxde
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/tier940/homelab.git
    targetRevision: main
    path: kubernetes/manifestes/application/lxde
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
