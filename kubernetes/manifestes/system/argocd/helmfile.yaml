repositories:
  - name: argo
    url: https://argoproj.github.io/argo-helm

helmDefaults:
  createNamespace: true
  wait: true
  timeout: 600

## Document separator to satisfy Helmfile:
---
releases:
  - name: argocd
    namespace: argocd
    createNamespace: true
    chart: argo/argo-cd
    version: 9.0.5
    values:
      - values/values.yaml
