repositories:
  - name: argo
    url: https://argoproj.github.io/argo-helm

helmDefaults:
  createNamespace: true
  wait: true
  timeout: 600

## Document separator to satisfy Helmfile:
---
namespace: argocd

releases:
  - name: argocd
    chart: argo/argo-cd
    version: 9.1.1
    values:
      - values/argocd.yaml
